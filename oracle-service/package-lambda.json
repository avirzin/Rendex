{
  "name": "rendex-oracle-lambda",
  "version": "1.0.0",
  "description": "CDI Oracle Service for Rendex Token - AWS Lambda Version",
  "main": "lambda-oracle.js",
  "scripts": {
    "build": "npm ci --production",
    "package": "zip -r lambda-oracle.zip . -x '*.zip' 'node_modules/.cache/*' '*.md' '*.git*'",
    "deploy": "npm run build && npm run package",
    "test": "node lambda-oracle.js"
  },
  "keywords": [
    "oracle",
    "cdi",
    "ethereum",
    "smart-contracts",
    "rendex",
    "lambda",
    "aws"
  ],
  "author": "Rendex Team",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "ethers": "^6.8.1"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
